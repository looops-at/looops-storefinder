{
  "name": "looops-storefinder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Looops Storefinder — HubSpot → stores.json sync + Shopify-embeddable Mapbox UI",
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "build": "node build.mjs",
    "dev": "node build.mjs --watch --serve"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "engines": {
    "node": ">=20"
  }
}
